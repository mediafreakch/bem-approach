module.exports = {
  install: function (Vue, options) {
    var store = options.store;

    Vue.prototype.$t = function(message, params = null, lang = 'default') {
      return this.$store.getters.locals[lang] ? this.$store.getters.locals[lang][message].format(params) : message;
    }
  }
};
