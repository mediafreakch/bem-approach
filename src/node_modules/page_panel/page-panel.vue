<template>
  <r-panel>
    A panel without header
  </r-panel>
  <r-panel title="A simple panel with title">
    Foo Panel!
  </r-panel>
  <r-panel title="A collapsible panel" type="collapsible">
    Foo
  </r-panel>
  <r-panel title="A panel with editable content" type="editable">
      <button slot="editable" @click="toggleEditMode()" v-show="!isEditMode">Edit</button>
      <div>
        <form v-if="isEditMode">
          <r-form-item
              variant="text"
              label="Name"
              input-id="name"
              type="text"
              placeholder="new name"
          ></r-form-item>
          <button @click="confirm">Confirm</button>
          <button @click="abort">Abort</button>
        </form>
        <template v-else>
          <label>Name</label>
          <input type="text" readonly value="Max Muster">
        </template>
      </div>
  </r-panel>
</template>

<script>
  var typo = require('ricardo-element_typo');
  var formItem = require('ricardo-element_form-elements').components.formItem;
  var panel = require('ricardo-component_panel');

  module.exports = {
    components: {
      "r-panel": panel,
      "r-form-item": formItem
    },
    data: function() {
      return {
        isEditMode: false
      }
    },
    methods: {
      toggleEditMode: function() {
        this.isEditMode = !this.isEditMode;
      },
      abort: function() {
        this.toggleEditMode();
      },
      confirm: function() {
        // do some fancy stuff like serializing the form
      }
    }
  };
</script>
