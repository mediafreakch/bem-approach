var Vue = require('vue'),
    VueResource = require('vue-resource'),
    API_ROOT = 'http://localhost:8085/api/';

Vue.use(VueResource);

Vue.http.options.crossOrigin = true;

module.exports = {
  UserResource: Vue.resource(API_ROOT + 'users{/id}')
};
